import classes from "../5. Resume/resume.module.css";

function Resume() {
    return (
        <div className={`${classes.div}`}>
            <h1>Resume</h1>
        </div>
    )
}

export default Resume;