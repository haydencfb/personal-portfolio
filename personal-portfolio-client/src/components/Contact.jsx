import classes from '../styles/Contact.module.css';

function Contact() {
    return (
        <div className={`${classes.contactDiv}`}>
            <h1>Contact</h1>
        </div>
    )
}

export default Contact;