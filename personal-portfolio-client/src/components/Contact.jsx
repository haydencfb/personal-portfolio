import classes from "../styles/main.module.css";

function Contact() {
    return (
        <div className={`${classes.div}`}>
            <h1>Contact</h1>
        </div>
    )
}

export default Contact;